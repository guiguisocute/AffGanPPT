// =====================================================================
// AffGAN 批注精读稿 —— 《Amortised MAP Inference for Image Super-Resolution》
// 定位：不是逐句翻译（逐句对照请看 AffGAN_zh.pdf），而是"把论文讲一遍"：
//       每节先给脉络，再意译原文要点，配术语卡 / 逐项拆解 / 人话直觉 / 易错点四类批注。
// =====================================================================

#set document(title: "AffGAN 批注精读稿", author: "精读批注")

#set page(
  paper: "a4",
  margin: (top: 2.2cm, bottom: 2.2cm, left: 2.1cm, right: 2.1cm),
  numbering: "1 / 1",
  number-align: center,
)

#set text(
  font: ("Source Han Serif SC", "Source Han Serif"),
  lang: "zh", region: "cn",
  size: 10.5pt,
)
#set par(justify: true, leading: 0.82em, spacing: 1.0em)

#show heading: set text(font: ("Source Han Serif SC", "SimHei"), weight: "bold")
#set heading(numbering: none)
#show heading.where(level: 1): it => {
  v(1.1em)
  block[
    #text(size: 16pt, fill: rgb("#1a4f8a"))[#it.body]
    #v(-0.4em)
    #line(length: 100%, stroke: 2pt + gradient.linear(rgb("#d97757"), white))
  ]
  v(0.2em)
}
#show heading.where(level: 2): it => {
  v(0.7em)
  block[#text(size: 12.5pt, fill: rgb("#2a3a4a"))[▍#it.body]]
  v(0.1em)
}
#show heading.where(level: 3): set text(size: 11pt)

#set math.equation(numbering: none)
#show link: set text(fill: rgb("#1a4f8a"))

// ---------- 数学算子 ----------
#let argmax = math.op("arg max", limits: true)
#let argmin = math.op("arg min", limits: true)

// ---------- 批注框体系 ----------
#let notebox(icon, title, bg, line, tcolor, body) = block(
  fill: bg,
  stroke: (left: 3pt + line),
  radius: (top-right: 4pt, bottom-right: 4pt),
  inset: (left: 11pt, right: 11pt, top: 8pt, bottom: 8pt),
  width: 100%, above: 0.95em, below: 0.95em, breakable: true,
)[
  #set text(size: 9.6pt)
  #set par(first-line-indent: 0em, leading: 0.72em, spacing: 0.7em)
  #text(weight: "bold", fill: tcolor)[#icon #title ▸] #h(0.35em) #body
]
// 本节脉络（灰）
#let flow(body) = notebox("🧭", "本节脉络", rgb("#f5f8fc"), rgb("#6b7785"), rgb("#3a4654"), body)
// 术语卡（蓝）
#let term(body) = notebox("📘", "术语卡", rgb("#eef4fb"), rgb("#2a6fb5"), rgb("#1a4f8a"), body)
// 人话直觉（暖橙）
#let idea(body) = notebox("💡", "人话直觉", rgb("#fdf1ec"), rgb("#d97757"), rgb("#c0563a"), body)
// 逐项拆解（绿）
#let walk(body) = notebox("🔍", "逐项拆解", rgb("#eef7f1"), rgb("#2f8a5b"), rgb("#22684a"), body)
// 易错点（红）
#let pit(body) = notebox("⚠️", "易错点", rgb("#fdeeee"), rgb("#c94b4b"), rgb("#a83232"), body)
// 延伸阅读（金）
#let more(body) = notebox("🔗", "延伸", rgb("#f5efe1"), rgb("#b39038"), rgb("#8a6d2f"), body)

// 原文大意（白底细框，意译非直译）
#let orig(body) = block(
  fill: white, stroke: 0.6pt + rgb("#c9d4e0"), radius: 4pt,
  inset: (left: 11pt, right: 11pt, top: 9pt, bottom: 9pt),
  width: 100%, above: 0.95em, below: 0.95em, breakable: true,
)[
  #set text(size: 10pt)
  #set par(first-line-indent: 0em, leading: 0.8em, spacing: 0.8em)
  #text(size: 8.5pt, weight: "bold", fill: rgb("#8593a3"), font: ("Source Han Serif SC", "SimHei"))[原文大意（意译）]
  #v(0.25em)
  #body
]

// 幻灯片锚点（内联小标签）
#let sl(n) = box(
  fill: rgb("#f5efe1"), stroke: 0.5pt + rgb("#dcc98f"), radius: 999pt,
  inset: (x: 6pt, y: 1.5pt),
)[#text(size: 8pt, fill: rgb("#8a6d2f"), font: ("Source Han Serif SC", "SimHei"))[▶ 幻灯片 #n]]

// 带论文编号的公式
#let peq(tag, m) = grid(
  columns: (1fr, auto), column-gutter: 8pt,
  align: (center + horizon, right + horizon),
  m, text(size: 9pt, fill: rgb("#8a6d2f"))[（论文式 #tag）],
)

// =====================================================================
// 封面
// =====================================================================
#align(center)[
  #v(1.4cm)
  #text(size: 11pt, fill: rgb("#8a6d2f"), font: ("Source Han Serif SC", "SimHei"))[ICLR 2017 · Oral　|　批注精读稿]
  #v(0.9cm)
  #text(font: ("Source Han Serif SC", "SimHei"), size: 25pt, weight: "bold")[
    用于图像超分辨率的\ 摊销 MAP 推断
  ]
  #v(0.4cm)
  #text(size: 12pt, fill: rgb("#666666"))[Amortised MAP Inference for Image Super-Resolution（AffGAN）]
  #v(0.8cm)
  #text(size: 10.5pt)[Casper Kaae Sønderby#super[1,2] · Jose Caballero#super[1] · Lucas Theis#super[1] · Wenzhe Shi#super[1] · Ferenc Huszár#super[1]]
  #v(0.2cm)
  #text(size: 9pt, fill: rgb("#777777"))[#super[1] Twitter（伦敦）　#super[2] 哥本哈根大学]
  #v(0.9cm)
  #box(
    fill: rgb("#eef4fb"), stroke: 0.7pt + rgb("#b9cfe6"), radius: 999pt,
    inset: (x: 18pt, y: 7pt),
  )[#text(size: 11pt, weight: "bold", fill: rgb("#1a4f8a"), font: ("Source Han Serif SC", "SimHei"))[全文一条主线：MAP → 仿射投影 → 交叉熵 → 三种解法]]
  #v(0.9cm)
  #line(length: 42%, stroke: 0.5pt + gray)
  #v(0.35cm)
  #text(size: 9.5pt, fill: rgb("#888888"))[
    本稿把整篇论文"讲"一遍：脉络 → 意译 → 批注 → 拆公式\
    配套：AffGAN_zh.pdf（逐句译文）· index.html（幻灯片）· 快速入门.html（冲刺手册）
  ]
]

#pagebreak()

// =====================================================================
// 使用说明
// =====================================================================
= 如何使用本稿

本稿按论文原有章节推进，但每一节都被重新组织成四层：先用一个#text(fill: rgb("#3a4654"), weight: "bold")[🧭 脉络框]告诉你"这节要解决什么、在主线上处于哪一步"；再用#text(fill: rgb("#8593a3"), weight: "bold")[原文大意框]把这一节的内容用中文意译讲清（不是逐句直译，逐句对照请翻 AffGAN_zh.pdf）；随后穿插四类批注；关键公式一律按论文编号引用并逐符号拆开。

*五类批注框的分工：*

#term[*术语卡*：第一次出现的概念在这里解释，不用往前翻。已经懂的直接跳过。]
#walk[*逐项拆解*：公式逐符号讲，推导补全论文跳过的中间步骤。第一遍读可跳过，第二遍必读。]
#idea[*人话直觉*：一句话版本。如果拆解框看晕了，回来读这里。]
#pit[*易错点*：最容易理解错、或讲论文时最容易说错的地方（部分条目来自对照原文逐句核验的勘误）。]
#more[*延伸*：与后续文献（如扩散模型）的联系，读不读不影响理解论文。]

*建议的三遍读法：*第一遍只读每节的脉络框＋人话框＋原文大意（约 40 分钟，建立骨架）；第二遍加读拆解框和易错点（重点在 §3，这一章占全文难度的七成）；第三遍带着文末"自测清单"回来检验，答不上的回到对应节。正文中的 #sl[12] 标签指向配套幻灯片 index.html 的页码，讲稿排练时可对照。

#v(0.4em)
#block(
  fill: rgb("#f5f8fc"), stroke: 0.6pt + rgb("#c9d4e0"), radius: 4pt, inset: 11pt, width: 100%,
)[
  #set par(first-line-indent: 0em)
  #set text(size: 9.6pt)
  #text(weight: "bold", font: ("Source Han Serif SC", "SimHei"))[全文主线（背下来）]
  #v(0.3em)
  ① *MAP*：超分是多解问题，MSE 取"平均"必然糊，改取后验*众数*；\
  ② *仿射投影*：用结构（而非损失项）把"输出下采样后必须等于输入"焊死，约束优化变无约束；\
  ③ *交叉熵*：于是"摊销 MAP"恰好等价于最小化模型输出分布与真实图像分布的交叉熵 $H[q_theta, p_Y]$，超分变成生成式建模，不再需要成对数据；\
  ④ *三种解法*：交叉熵没法直接算 → 用 GAN（AffGAN）、去噪梯度（AffDG）、显式密度（AffLL）三种方式逼近，实验中 AffGAN 胜出。
]

#v(0.4em)
#outline(title: [目　录], depth: 2)

#pagebreak()

// =====================================================================
// 标题与摘要
// =====================================================================
= 第 0 站 · 标题拆解与摘要精读

== 标题四个词，就是论文的全部承诺

#walk[
  *Amortised（摊销的）*——本意是会计术语"分期摊还"。机器学习里指：不再对每个输入单独跑一遍优化，而是*把优化的成本一次性付在训练里*，训练出一个函数，测试时前向一次直接给出（近似）最优解。#sl[10]

  *MAP（Maximum a Posteriori，最大后验）*——在所有解释得通的解里，取后验概率 $p(y|x)$ 最大的那一个，即后验分布的*众数（mode）*。与之相对：MSE 训练对应后验*均值*，MAE 对应*中位数*。#sl[6]

  *Inference（推断）*——这里指从观测 $x$ 反推隐藏量 $y$ 的过程。合起来，"摊销 MAP 推断" = 训练一个网络，一步输出 MAP 风格的解。

  *Image Super-Resolution（图像超分辨率，SR）*——从低分辨率（LR）图像估计高分辨率（HR）图像。核心麻烦：这是个*欠定逆问题*，一个输入对应无穷多合法输出。

  *AffGAN* 这个名字 = *Affine projection*（仿射投影）＋ *GAN*。论文三种方法统一以前缀命名：Aff- 表示带投影，Soft- 表示只用软约束；后缀 GAN / DG（Denoiser-Guided）/ LL（Log-Likelihood）表示用哪种武器逼近交叉熵。
]

== 摘要的信息结构

#orig[
  摘要五步走：① *问题*——SR 欠定，大量合理的 HR 图像都能解释同一张下采样图；② *现状批判*——主流方法用经验风险最小化＋逐像素 MSE，输出模糊、过度平滑、不真实；③ *主张*——更可取的是 MAP 推断：偏好在图像先验下概率高、因而看起来可信的解；但直接做 MAP 很难，因为要从样本建出图像先验；④ *方案*——提出摊销 MAP 推断：先用一个新架构把网络输出投影到"有效 SR 解的仿射子空间"（保证输出与输入一致），再证明此时摊销 MAP 归约为最小化两个分布的交叉熵（类似训练生成模型），最后给出三种求解办法：GAN、去噪器引导、最大似然先验基线；⑤ *结论*——GAN 版本在真实图像上最好，纹理超分尤其出色。
]

#idea[
  一句话浓缩：*"多解问题别取平均，去爬概率峰顶；爬峰顶这件事，可以改写成生成式建模，然后交给 GAN。"*
]

#pit[
  摘要没有告诉你的三件事，读完全文才会补齐：① 整套一致性保证建立在*无噪线性观测模型* $x = A y$ 且 $A$ 已知之上（§3.1）；② GAN 实际最小化的并不是交叉熵本身，而是差一个熵项的 KL 散度（§3.2）；③ 作者自己在 §5.6 对"MAP 是否真是我们想要的"泼了冷水（高维下众数未必典型）。读摘要时留个心眼，别把方法边界读丢了。
]

// =====================================================================
// §1 引言
// =====================================================================
= 第 1 站 · §1 引言：为什么放弃 MSE、转向 MAP

#flow[
  引言做三件事：①把 SR 定义成欠定逆问题，指出 MSE 的失败是*贝叶斯意义上注定的*，不是网络不够大；②交代"换感知损失"这条路为什么暂时走不通；③用一个二维玩具（Figure 1）把"MAP 优于均值/中位数"画给你看，并预告全文两大贡献。主线位置：第 ① 步"为什么要 MAP"。#sl[4] #sl[5] #sl[6] #sl[7] #sl[8]
]

== 1.1 欠定逆问题与"平均必糊"

#orig[
  SR 的任务是给定 LR 输入估计 HR 图像。它欠定：以 4× 超分为例，输出像素数是输入的 16 倍，大量 HR 图像下采样后得到同一张 LR 图。主流方法做经验风险最小化（多用逐像素 MSE），而 MSE 这类凸损失面对多峰分布（比如自然图像分布）时有个已知缺陷：贝叶斯最优行为是输出所有合理解的*后验加权平均*。对自然图像来说，这种平均导致模糊、过度平滑、看起来不真实——即产出的估计在自然图像先验下概率很低。
]

#term[
  *欠定（underdetermined）*：未知数多于约束，解不唯一。*逆问题（inverse problem）*：由结果（LR 图）反推原因（HR 图）。*经验风险最小化（ERM）*：在训练对 $(x, y)$ 上最小化平均损失 $bb(E)[ell(y, f_theta (x))]$，即"有标准答案的监督回归"。
]

#walk[
  "MSE 最优解 = 后验均值"是本文一切动机的地基，补上论文省略的一行证明：固定 $x$，在所有可能输出 $a$ 中最小化 $bb(E)_(y|x) norm(y - a)_2^2$，对 $a$ 求导置零得 $a^* = bb(E)[y|x]$，即后验均值。同理可证 MAE 的最优解是后验中位数、0–1 损失（离散情形）的最优解是众数。所以：*网络训练得再完美，MSE 也只会让它输出"平均图"*——两种都合理的纹理逐像素平均后，高频细节相互抵消，只剩模糊底色。#sl[6]
]

== 1.2 为什么不直接换"感知损失"

#orig[
  理想方案是用一个全参考感知损失刻画人眼对失真的敏感度。但最常用的 MSE 及其亲戚 PSNR 与人的画质感知相关性差；改进指标（SSIM、MS-SSIM 及后续工作）虽有进步，仍不足以作为训练神经网络的满意替代。于是本文干脆离开经验风险最小化框架，只依赖自然图像统计来做文章。
]

#idea[
  这段话是全文的"岔路口"：别人修损失函数，本文换问题表述——不问"输出离标准答案多近"，改问"输出像不像一张真实图像、且是否与输入一致"。后面所有数学都是把这句话形式化。
]

== 1.3 Figure 1：全文题眼，务必读透

#orig[
  玩具设定：HR 数据是二维点 $y = [y_1, y_2]$，从瑞士卷分布中采样；下采样定义为两像素取平均 $x = (y_1 + y_2)\/2$。观测到 $x = 0.5$ 时，所有合法解构成直线 $y_2 = 1 - y_1$（更一般地：一个仿射子空间）。后验 $p(y|x)$ 因此退化为"先验沿这条线的切片"。最小化 MSE / MAE 的贝叶斯最优解分别落在这条线上的均值 / 中位数处——它们可能位于先验密度很低的地方；而 MAP 推断按定义找众数，必然落在高概率区。图 b 进一步把 $x$ 从 $-8$ 扫到 $8$，画出各训练模型的输出轨迹：AffGAN 与 AffDG 紧贴后验众数，MSE 与 MAE 的输出则频繁穿越低概率区域。
]

#walk[
  把这张图拆成四步读：#sl[7]

  *① 约束*——下采样是线性算子：$x = A y$，这里 $A = [1\/2, 1\/2]$ 是 $1 times 2$ 矩阵。给定 $x$，解集 ${y : A y = x}$ 是一条直线（高维时是仿射子空间）。*低清图给的不是答案，是一个约束。*

  *② 后验*——先验 $p_Y$（灰色瑞士卷，越深密度越高）沿约束线切一刀，得到红色阴影的一维后验。

  *③ 选点*——同一条后验，三种损失给出三个不同的"最优点"：均值可能落在两卷之间的空地（两个峰平均出一个谁都不像的点）；中位数是折中；众数落在红色最深处。

  *④ 验证*——图 b 中黑色小箭头是*用去噪函数估计出的 $nabla_y log p_Y$ 梯度场*（§3.3 的伏笔：箭头都指向瑞士卷高密度处）；彩色轨迹是各模型对不同 $x$ 的输出。
]

#pit[
  Figure 1 的两个经典读图错误：① *图中彩色点不是数据点，而是各方法的超分输出*；② 图 a 和图 b 都有蓝色点，但含义不同——图 a 的蓝点是暴力求出的 MAP 解，图 b 的蓝点是训练出的 AffGAN 输出（红 = MSE，绿 = MAE，紫 = AffDG）。讲解时务必提醒听众。
]

#term[
  *仿射子空间（affine subspace）*：线性子空间平移后的结果——过原点的叫线性子空间，不过原点的叫仿射子空间。解集 ${y: A y = x}$ 当 $x eq.not 0$ 时不过原点，故称"仿射"。这个词就是 AffGAN 里 "Aff" 的出处。
]

== 1.4 引言承诺的两大贡献

#orig[
  贡献一：一种利用 SR 问题结构的 CNN 架构——下采样是线性变换（可建模为带步长的卷积），与任意 LR 图像相容的 HR 图像张成仿射子空间；用特定选择的线性卷积与反卷积层即可实现向该子空间的投影，从而保证网络输出恒与输入一致。该投影层可以加到任何 CNN、乃至任何可训练的 SR 算法上。贡献二：在此架构下，为 MAP 推断训练模型归约为最小化 HR 数据分布 $p_Y$ 与模型输出分布 $q_G$ 之间的交叉熵 $H[q_G, p_Y]$——于是不再需要成对的 HR–LR 数据，训练更像训练生成模型。据此给出三条逼近路线：GAN（并顺带提出稳定 GAN 训练的通用技巧"实例噪声"）、去噪器引导（贝叶斯最优去噪器近似给出对数密度梯度，可反传训练网络）、以及最大似然密度模型基线（PixelCNN + MCGSM）。
]

#idea[
  记住论文的"承诺清单"，后面每一节都是在兑现：投影层（§3.1）→ 交叉熵化归（§3.1 末）→ AffGAN＋实例噪声（§3.2）→ AffDG（§3.3）→ AffLL（§3.4）→ 实验逐项验证（§5）。
]

// =====================================================================
// §2 相关工作
// =====================================================================
= 第 2 站 · §2 相关工作：本文站在哪三条线的交点上

#flow[
  相关工作按三条线读：① GAN 理论线——本文的修正更新规则在"GAN 到底在最小化什么散度"谱系中的位置；② GAN 做 SR 线——与 SRGAN 等工作的分工与区别；③ 去噪 = 梯度线——AffDG 的理论血统。#sl[16]
]

#orig[
  *GAN 理论线*：Goodfellow 等（2014）提出 GAN 框架，并证明在特定条件下其训练最小化 $q_G$ 与 $p_Y$ 间的 Jensen–Shannon 散度。本文 §3.2 给出的更新规则对应最小化 $"KL"[q_G bar.v.double p_Y]$（该结论最早见于 Huszár 2016 的一篇未发表笔记）。Nowozin 等（2016）的 f-GAN 把 GAN 推广到一般 f-散度极小化；与本文平行，Mohamed 与 Lakshminarayanan（2016）给出隐式生成模型学习的统一视角，本文变体可视作其特例。近期多篇工作聚焦稳定 GAN 的算法技巧（DCGAN、Salimans 等 2016）；本文 §3.2.1 贡献另一个技巧——实例噪声，并与单边标签平滑对比；Arjovsky 与 Bottou（2017）在平行工作中提出了类似方法。

  *GAN 做 SR 线*：Bruna 等（2016）与 Li & Wand（2016）在预训练分类网络的特征空间里量距离；Dosovitskiy & Brox（2016）与 Ledig 等（2016，即 SRGAN）在类似思路上再加对抗损失；Garcia（2016）的未发表工作探索 GAN 加"LR 输入与下采样输出之间的 L1 惩罚"。本文指出：这些方法中的软 L2 / L1 惩罚可以解释为假设了高斯 / 拉普拉斯观测噪声；而本文假设无观测噪声，用仿射投影*精确*满足输入–输出一致性。另外 Larsen 等（2015）把 VAE 的逐像素 MSE 换成 GAN 判别器学到的度量。

  *去噪 = 梯度线*：概率建模与去噪学习之间有基本联系（Vincent 等 2008；Vincent 2011；Särelä & Valpola 2005；Alain & Bengio 2014；Rasmus 等 2015）：贝叶斯最优去噪器可用来估计数据对数概率的梯度。据作者所知，把去噪器的输出*显式反传去训练另一个网络*，本文是第一次。
]

#walk[
  "软惩罚 = 假设观测噪声"这句是相关工作里最有信息量的一句，展开讲：若假设 $x = A y + epsilon$、$epsilon$ 为高斯噪声，则对数似然 $log p(x|y) prop -norm(x - A y)_2^2 \/ (2 sigma^2)$——正是 L2 软惩罚项；换拉普拉斯噪声就得到 L1（即 Garcia 用的 MAE 项）。反过来读：*你在损失里加的每个软约束，都对应一个隐含的噪声假设*。本文说"我假设无噪声"，于是软惩罚升级为硬投影，一致性从"被鼓励"变成"被保证"。#sl[16]
]

#pit[
  与 SRGAN 的关系别说成"打败了 SRGAN"：论文没有与 SRGAN 做直接数值对比，两者是互补关系——SRGAN 是"对抗＋感知损失"的工程组合、依赖成对数据；本文为"在 SR 里用生成式目标"补充了理论动机（摊销 MAP），并把一致性从内容损失的副产品变成结构保证。论文自己的措辞是：我们的分析为在图像 SR 中使用 GAN（Ledig 等 2016）提供了理论依据。
]

#more[
  时间线冷知识：单边标签平滑（Salimans 等）与本文同为 2016 年工作，"实例噪声 vs 标签平滑"是同期两种药方之争；Arjovsky & Bottou（2017）沿同一病因分析走向了 Wasserstein GAN 的前奏。去噪线的思想后来在 score matching / 扩散模型中大放异彩——见 §3.3 的延伸框。
]
// =====================================================================
// §3 理论
// =====================================================================
= 第 3 站 · §3 理论：从 MAP 到交叉熵的完整推导

#flow[
  这是全文最硬、也最值得逐行读的一章，占理解难度的七成。路线图：先把"摊销 MAP"写成一个优化目标（式 1–4），再用观测模型把似然项变成硬约束（式 5–6），用仿射投影把硬约束吃进网络结构（式 7），最后发现剩下的目标恰好是交叉熵（式 8–9）——至此"超分"正式变成"生成式建模"。随后三小节给出三件武器。#sl[10] #sl[11] #sl[12] #sl[13]
]

== 3.0 从经验风险到摊销 MAP（式 1–4）

#orig[
  设 $f_theta (x)$ 是把 LR 观测 $x$ 映到 HR 估计 $hat(y)$ 的参数化函数。多数现有方法通过经验风险最小化学习参数：
]
#peq[1][$ argmin_theta bb(E)_(y,x) [ell(y, f_theta (x))] $]
#orig[
  其中 $ell$ 通常取 MSE。本文改做 MAP 推断。对单个观测 $x$，MAP 估计为：
]
#peq[2][$ hat(y)(x) = argmax_y log p_(Y|X) (y|x) $]
#orig[
  与其对每个 $x$ 单独求解式 (2)，本文做*摊销推断*：训练 $f_theta$ 直接输出 MAP 估计。自然的训练目标是平均对数后验：
]
#peq[3][$ argmax_theta bb(E)_x log p_(Y|X) (f_theta (x)|x) $]
#orig[
  期望取在 LR 观测的分布上。该目标依赖未知的后验 $p_(Y|X)$，于是用贝叶斯公式把对数后验分解为三项：
]
#peq[4][$ argmax_theta {underbrace(bb(E)_x log p_(X|Y)(x|f_theta (x)), "似然") + underbrace(bb(E)_x log p_Y (f_theta (x)), "先验") - underbrace(bb(E)_x log p_X (x), "边缘似然")} $]

#walk[
  三个式子的递进关系值得慢读：式 (2) 是"每张图跑一遍优化"（慢、非凸、每次都重来）；式 (3) 把 argmax 从"对 $y$"换成"对 $theta$"——*优化对象从解本身变成产生解的函数*，这就是摊销。式 (4) 三项各管一摊：*似然项*管"输出跟输入对不对得上"（数据一致性）；*先验项*管"输出像不像一张真实图像"（自然性）；*边缘似然项* $log p_X (x)$ 与 $theta$ 完全无关，直接扔掉。剩下的两项，恰好对应后面两大发明：似然项 → 仿射投影（§3.1），先验项 → 交叉熵与三种武器（§3.1 末–§3.4）。#sl[10]
]

#idea[
  摊销 = "把逐张优化的钱，一次付在训练里"。训练贵一点，换来测试时前向一次出图。
]

#pit[
  式 (3) 不是一个新损失函数替换 MSE 那么简单：它的期望*只对 $x$ 取*，公式里根本没有成对的真值 $y$ 出现——这个细节预告了后面"不需要配对数据"的结论。对照式 (1) 里 $bb(E)_(y,x)$ 的双下标，差别一目了然。
]

== 3.1 处理似然项：仿射投影层（式 5–7）

#orig[
  SR 的观测模型写作
]
#peq[5][$ x = A hat(y) $]
#orig[
  其中 $A$ 是用于下采样的线性变换，一般可建模为带步长的二维卷积。因此式 (4) 中的似然项是退化的：$p(x|f_theta (x)) = delta(x - A f_theta (x))$，式 (4) 变成约束优化：
]
#peq[6][$ argmax_(theta, thick forall x: A f_theta (x) = x) bb(E)_x [log p_Y (f_theta (x))] $]
#orig[
  为满足约束，引入一族*天生满足* $A f_theta (x) = x$ 的函数：
]
#peq[7][$ g_theta (x) = Pi_x^A f_theta (x) = (I - A^+ A) f_theta (x) + A^+ x $]
#orig[
  其中 $f_theta$ 是任意 LR→HR 映射，$Pi_x^A$ 是向仿射子空间 ${y : A y = x}$ 的投影，$A^+$ 是 $A$ 的 Moore–Penrose 伪逆（满足 $A A^+ A = A$ 与 $A^+ A A^+ = A^+$）。若 $A$ 是带步长的卷积，$A^+$ 就是一次反卷积/上卷积——深度学习的标准操作；但*最优的 $A^+$ 并不是 $A$ 的转置*，本文用数值方法求解（附录 B）。直觉上，$A^+ x$ 可视为一个基线 SR 解，$(I - A^+ A) f_theta$ 是残差；算子 $(I - A^+ A)$ 恰是向 $A$ 的零空间的投影，所以无论 $f_theta$ 输出什么，残差下采样后必为 0。
]

#term[
  *δ 分布（Dirac delta）*：把全部概率压在一个点上的"极限分布"。"似然是 δ"意味着：观测无噪声，$y$ 要么精确解释 $x$（下采样后相等），要么概率为零——没有中间地带。*零空间（null space）*：被 $A$ 映成 0 的向量集合 ${v : A v = 0}$；沿零空间方向移动，下采样结果不变。*Moore–Penrose 伪逆*：不可逆（非方阵）矩阵的"最稳反操作"，此处只用到 $A A^+ A = A$ 这一条性质。#sl[11]
]

#walk[
  式 (7) 是全文最硬核的一行，逐块拆：

  *块① $A^+ x$（基线解）*——把 LR 图直接"抬"回 HR 空间的一个特解。验证它合法：$A (A^+ x) = A A^+ A y = A y = x$（第二个等号用了 $x = A y$ 与 $A A^+ A = A$）。它保证低频内容正确，但没有高频细节。

  *块② $(I - A^+ A) f_theta (x)$（零空间残差）*——网络的全部自由度。验证它"下采样后隐形"：$A (I - A^+ A) f_theta = (A - A A^+ A) f_theta = (A - A) f_theta = 0$。

  *合起来*：$A g_theta (x) = 0 + x = x$，一致性对任意 $theta$、任意输入恒成立——*是结构强制的，不是损失鼓励的*。几何图景：整个仿射子空间是一张"合法解平面"，$A^+ x$ 是平面上的锚点，网络输出被投影回平面上离它最近的点（用 Moore–Penrose 伪逆时该投影恰为正交投影）。#sl[12]
]

#idea[
  一句话：*底图管对得上，残差管好不好看；残差被锁在"下采样隐形"的方向里，怎么折腾都不会破坏一致性。*
]

#pit[
  三个高频口误：① $A^+ eq.not A^tack.b$——伪逆的卷积核要数值求解（附录 B），Figure 2d 可视化了两者：$A$ 是固定的高斯核，$A^+$ 却带负瓣，长得像锐化核；② $A$ *从头到尾是固定的*（9×9 高斯核、步长 4），被学习/优化的只有 $A^+$ 的初始化（且训练中冻结或微调均可，见 §5.2）；③ 这一切成立的前提是无噪线性观测模型且 $A$ 已知——被问"真实退化未知怎么办"时，答案是"超出本文设定，软约束版可作退路但效果更差"。
]

== 3.1 续：化归交叉熵（式 8–9）——全文最漂亮的一步

#orig[
  用式 (7) 的函数族后，式 (6) 变成无约束优化：
]
#peq[8][$ argmax_theta bb(E)_x log p_Y (Pi_x^A f_theta (x)) $]
#orig[
  有趣的是，这个目标可以用"模型输出的概率分布" $q_theta (y) := integral delta(y - Pi_x^A f_theta (x)) thick p_X (x) dif x$ 改写为：
]
#peq[9][$ argmax_theta bb(E)_x log p_Y (Pi_x^A f_theta (x)) = argmax_theta bb(E)_(hat(y) tilde.op q_theta) log p_Y (hat(y)) = argmin_theta H[q_theta, p_Y] $]
#orig[
  其中 $H[q, p]$ 是交叉熵，且 $H[q_theta, p_Y] = bb(E)_(hat(y) tilde.op q_theta)[-log p_Y (hat(y))]$。最小化这个目标*不需要匹配的输入–输出对*，只需让"重建图像的边缘分布 $q_theta$"匹配"HR 图像分布 $p_Y$"——问题从此更像无监督学习或生成式建模。
]

#walk[
  $q_theta$ 的定义式看着吓人，其实是句大白话：*把所有低清图（按 $p_X$ 的出现频率）过一遍网络，收集全部输出，这些输出构成的分布就是 $q_theta$。*式 (9) 的等号也只是"换个记账方式"：对 $x$ 取期望再算 $log p_Y ("输出")$，等于直接对"输出"取期望算 $log p_Y$——因为输出由 $x$ 唯一决定（网络是确定性函数）。最后一个等号是交叉熵的定义。三个等号没有任何近似，*严格成立*。#sl[13]
]

#idea[
  翻译成人话：*"让每张输出图在真实图像先验下尽量高概率" = "让输出人群整体搬进真实图像的高密度社区"。*从此训练只需要三样东西：一堆低清图（不需要对应答案）、一堆高清图（不需要对应输入）、已知的 $A$。
]

#pit[
  两个细节别读过头：① 交叉熵低 ≠ 两分布匹配——$H[q, p] = H[q] + "KL"[q bar.v.double p]$，它还含 $q$ 自身的熵，最小化交叉熵会*额外*偏好低熵（塌向众数）的解，这正是"MAP 味"的来源，也是下一节 GAN 替代目标"差一个熵项"讨论的伏笔；② 式 (9) 仍然*没法直接算*——$p_Y$ 是未知的自然图像分布，这就是需要三种武器的原因。
]

== 3.2 武器一 · AffGAN：仿射投影 + 修正更新规则（式 10）

#orig[
  GAN 由生成器 $G$（把噪声 $z tilde.op p_Z$ 映成图像）与判别器 $D$（区分真假图像）组成，两者交替更新，使生成分布 $q_G$ 逼近真实分布 $p_Y$。GAN 的行为取决于训练细节，本文采用如下目标：
]
#peq[10][$ cal(L)(D; G) = -bb(E)_(y tilde.op p_Y) log D(y) - bb(E)_(z tilde.op p_Z) log(1 - D(G(z))), quad quad cal(L)(G; D) = -bb(E)_(z tilde.op p_Z) log frac(D(G(z)), 1 - D(G(z))) $]
#orig[
  算法交替两步：固定 $G$ 降低 $cal(L)(D;G)$，再固定 $D$ 降低 $cal(L)(G;D)$。可以证明这相当于最小化 $"KL"[q_G bar.v.double p_Y]$（证明见附录 A，结论最早由 Huszár 2016 给出）。在 SR 语境下，*带仿射投影的 SR 函数 $Pi_x^A f_theta$ 充当生成器，输入不再是噪声而是低清图 $x tilde.op p_X$*，其余保持不变，即可用 GAN 算法最小化 $"KL"[q_theta bar.v.double p_Y]$——此法名为 AffGAN。对照组 SoftGAN 去掉投影，改用软约束 $ell_"LR" = "MAE"(x, A hat(y))$。注意交叉熵与 KL 之差恰为 $q_theta$ 的熵：$H[q_theta, p_Y] - "KL"[q_theta bar.v.double p_Y] = H[q_theta]$，因此可以预期 AffGAN 偏好熵更高、整体上更多样的近似 MAP 解。
]

#walk[
  式 (10) 两行分工明确：第一行是*判别器损失*——标准二分类交叉熵（真图打 1、假图打 0），没有任何改动。第二行是*生成器损失*，这才是修改所在：原版 GAN 生成器最小化 $log(1 - D)$（或实践中最大化 $log D$），本文让生成器最小化 $-log frac(D, 1-D)$——把两者*合并成对数几率（logit）*。差别的后果：原版 minimax 在最优判别器下对应最小化 JS 散度，修正版恰好对应最小化*反向 KL* $"KL"[q_theta bar.v.double p_Y]$（推导见附录 A 精读）。而反向 KL 与我们真正想要的交叉熵只差一个熵项——这是"用 GAN 逼近 MAP"在数学上成立的关键一环。#sl[15] #sl[17]
]

#idea[
  熵差那行公式值得单独记：*想要的（交叉熵）− 实际最小化的（KL）= 熵奖励 $H[q_theta]$*。纯交叉熵会把一切压向众数；KL 版本额外奖励输出分布的多样性。论文原话到"偏好更高熵、更多样的解"为止；"因此有助于缓解 mode collapse"是顺理成章的引申，但*论文没有直接这么说*，讲的时候注意区分。#sl[18]
]

#pit[
  "多样性"指的是*边缘输出分布 $q_theta$ 跨不同输入的多样性*：这个模型是确定性的，同一张 $x$ 永远只输出同一张图。想要"同一输入多个解"，要等附录 F 的随机版扩展。
]

== 3.2.1 实例噪声：GAN 为什么不稳、怎么救（式 26）

#orig[
  理论上 GAN 应当收敛：若最优判别器唯一且每步都训到最优，整个算法就是对 $"KL"[q_theta bar.v.double p_Y]$ 估计值的梯度下降。实践中 GAN 却出名地不稳。哪里出了问题？作者认为主因是 $q_theta$ 与 $p_Y$ 都是*集中*的分布，支撑几乎不重叠：自然图像分布通常被认为集中在低维流形附近，而 $q_theta$（如 AffGAN 中）按构造就是退化、类流形的。于是尤其在收敛前，存在一大批都能完美区分两者的判别器，破坏了收敛性证明的前提。解决办法：给 SR 输出和真实图像*同时*加噪声（实例噪声），等价于最小化
]
#peq[26][$ d_sigma (q_theta, p_Y) = "KL"[p_sigma ast q_theta thick bar.v.double thick p_sigma ast p_Y] $]
#orig[
  其中 $p_sigma ast q$ 表示分布与噪声分布的卷积。噪声强度 $sigma$ 随训练退火；加噪后可以放心把 $D$ 训到收敛。该技巧与 Salimans 等（2016）的单边标签平滑相关，但*不会给最优判别器引入偏差*，作者认为它是稳定 GAN 训练的通用候选。细节见附录 C。
]

#walk[
  病根拆成三条（附录 C 展开，这里先给结论）：支撑不重叠时，① 对数似然比 $log q_theta \/ p_Y$ 处处无穷 → KL 失去定义；② JS 散度饱和在最大值、对 $theta$ 局部恒定 → 没有梯度；③ 逻辑回归损失接近最优的判别器有一大堆，每个给生成器的梯度方向各不相同 → 训练随初始化漂移。实例噪声一招治三症：卷积把两个"细线"分布抹宽，支撑重叠 → 似然比有限、JS 不再饱和、最优判别器唯一。$sigma$ 退火到 0 时回到原问题。#sl[19]
]

#idea[
  比喻：两条几乎不相交的细线，有无数种画法能把它们分开；给两条线都"吹一层气"变成两团重叠的雾，分界线就只剩一条了。
]

#more[
  这一小节是本文影响力最大的副产品：与 Arjovsky & Bottou（2017）的平行分析共同构成了后来 WGAN 系列的病因诊断；"对样本加噪并退火"的做法与扩散模型的噪声调度直觉相通——但目的不同：实例噪声是为了*稳定判别器*，扩散模型的加噪定义了*生成过程本身*。同源不同用。
]

== 3.3 武器二 · AffDG：去噪器引导（式 11–12）

#orig[
  要用梯度下降优化式 (6)，需要目标对 $theta$ 的梯度：
]
#peq[11][$ frac(partial, partial theta) bb(E)_x [log p(Pi_x^A f_theta (x))] = bb(E)_x [underbrace(nabla_y log p(y) bar.v_(y = Pi_x^A f_theta (x)), "得分项（score）") dot underbrace(Pi_x^A frac(partial, partial theta) f_theta (x), "网络梯度经投影")] $]
#orig[
  网络梯度可反向传播求得，但得分项 $nabla_y log p_Y (y)$ 依赖未知的 $p_Y$，需要估计。借用 Alain & Bengio（2014）等的结论：在无穷小高斯噪声极限下，最优去噪函数可用于估计该梯度——
]
#peq[12][$ f_sigma^* = argmin_f bb(E)_(y tilde.op p_Y) ell_"MSE" (f(y + sigma epsilon), y) quad arrow.r.double quad frac(f^*(y) - y, sigma^2) approx nabla_y log p_Y (y) $]
#orig[
  其中 $epsilon tilde.op N(0, I)$ 为高斯白噪声。于是流程是：先训练一个神经网络对来自 $p_Y$ 的样本去噪，再把式 (12) 的梯度估计经式 (11) 的链式法则反传，更新 $theta$。方法命名 AffDG（仿射投影 + Denoiser-Guided）；软约束对照组称 SoftDG。
]

#walk[
  式 (12) 的直觉推导（论文引用文献，这里补一版直觉）：给真图 $y$ 加噪得 $tilde(y)$，最优去噪器输出的是"看到 $tilde(y)$ 时干净图的后验均值"。噪声很小时，这个后验均值相对 $tilde(y)$ 的偏移方向，恰好指向"附近密度更高的地方"——因为噪声把点均匀地推离原位，而"拉回去"的最优方向就是密度上升最快的方向。于是 *去噪残差 ÷ $sigma^2$ ≈ 对数密度梯度（score）*。适用条件别丢：*小噪声极限下的近似*，所以训练要做 $sigma$ 退火。#sl[20] #sl[21]

  训练两阶段（附录 D 式 27–30）：*阶段一*预训练去噪自编码器（DAE），期间退火 $sigma$ 并保存各噪声水平下的参数快照；*阶段二*训练 SR 网络——输出 $hat(y)$ 喂给 DAE 拿梯度估计、按链式法则反传做梯度*上升*；期间 DAE 不被 SR 损失更新，但会按退火计划*换用更低噪声水平的参数快照*：大噪声版本梯度粗但覆盖广（训练早期用），小噪声版本梯度准但只在流形附近有效（后期用）。
]

#idea[
  一句话：*不用写出先验密度，训练一个去噪器就等于拿到了"往更像真图的方向走"的指南针。*论文自述这是首次把去噪器输出显式反传去训练另一个网络。
]

#more[
  这条线是今天 score-based / 扩散模型的直系祖先之一（Vincent 2011 的 denoising score matching → 本文的去噪引导 → Song & Ermon 2019 的 NCSN → DDPM）。回答"和扩散模型什么关系"时的标准句式：*思想同源（去噪 ≈ 估计 score），流程不同（这里是用冻结去噪器引导前馈网络，不是多步加噪-去噪的生成链）*。
]

== 3.4 武器三 · AffLL：显式密度模型（式 31–32）

#orig[
  最直接的基线：用最大似然拟合一个"可处理但强大"的密度模型逼近 $p_Y$，再用生成样本在该模型下的交叉熵近似式 (9)。具体用类 PixelCNN 的结构（Oord 等 2016），但把似然换成*连续且可微*的 MCGSM（条件高斯尺度混合的混合，Theis 等 2012）。命名 AffLL（仿射投影 + Log-Likelihood 引导）。训练分两步：先按链式分解 $L(y) = sum_j log p(y_j | y_(<j))$ 最大化密度模型的对数似然（式 31），再最小化生成样本的负对数似然 $ell = -L(f_theta (x))$（式 32）来训练 SR 网络。
]

#walk[
  为什么非换 MCGSM 不可：原版 PixelCNN 把每个像素建成 256 类的*离散分类分布*——对输入图像不可导，没法把"先验打分"的梯度传回 SR 网络。MCGSM 是连续可微的密度，才能当"可反传的打分器"。#sl[22]
]

#idea[
  三种武器一句话对比：AffGAN 让*判别器*当裁判（隐式、对抗式）；AffDG 让*去噪器*当指南针（只给梯度方向）；AffLL 让*密度模型*当打分器（显式打分）。理论上越靠后越"正统"，实验里却越靠前越能打——原因在 §5.3 与附录 E。
]
// =====================================================================
// §4 实验设计
// =====================================================================
= 第 4 站 · §4 实验设计：三个问题、四组数据

#flow[
  实验章很短，但设计意图值得先看清：作者把整个实验组织成对三个问题的回答，难度从二维玩具一路爬到真实自然图像。#sl[23]
]

#orig[
  实验回答三个问题：① §3 的方法真的在最小化交叉熵吗？→ §5.1；② 仿射投影层会不会损害 CNN 的超分性能？→ §5.2；③ 这些方法能产出感知上更好的超分结果吗？→ §5.3–5.5。先在"可以暴力算出精确 MAP"的二维瑞士卷上验证理论，再在真实图像上做 4× 超分：草地纹理（对 MSE/MAE 出名地难）、CelebA 人脸、ImageNet 自然图像。全部模型是卷积网络，用 Theano + Lasagne 实现。
]

#walk[
  实验设置速查（汇总自 §4 与附录 D，讲稿被追问时用）：

  #table(
    columns: (auto, 1fr, 1fr),
    inset: 6pt,
    stroke: 0.5pt + rgb("#c9d4e0"),
    align: left,
    table.header([*实验*], [*数据与退化*], [*模型要点*]),
    [瑞士卷], [二维点 $y = [cos(nu_1) r, thick sin(nu_1) r]$，$r = 0.4 nu_1 + nu_2$，$nu_1 tilde.op N(10, 3)$，$nu_2 tilde.op N(0, 0.2)$；$x = (y_1 + y_2)\/2$], [生成器/判别器均为 2 层 64 单元全连接网络；DAE 为 2 层 256 单元，噪声从 0.5 退火到 0.25],
    [草地纹理], [高分辨率草地图随机裁剪，4×], [生成器 6 层卷积（32→128 滤波器，隔层跳连）；判别器 4 层步长卷积],
    [CelebA], [中心裁剪缩放到 64×64，再用 $A$ 降到 16×16], [生成器 12 层卷积、判别器 8 层；标准训练/验证/测试划分],
    [ImageNet], [中心裁剪缩放到 128×128，降到 32×32；剔除小于 20kB 的图], [生成器 8 层、判别器 8 层；*必须*加实例噪声（$sigma$ 从 0.1 线性退火到 0）才能稳定训练],
  )

  所有图像实验共用的观测模型：$A$ = 固定的 9×9 高斯平滑核、步长 4 的卷积（即 4× 下采样）；$A^+$ = "16 个 5×5 卷积核 + 亚像素重排"（Shi 等 2016 的 sub-pixel 卷积），核参数按附录 B 数值优化。所有下采样都用这个 $A$ 完成。生成器带 ReLU、批归一化与跳连，输出接 sigmoid 后进投影层。
]

#pit[
  "ImageNet 上不加实例噪声就训不动"这句出现在附录 D 里，很容易漏读——它是实例噪声价值最硬的实验证据，讲稿里值得点一句。
]

// =====================================================================
// §5 结果与讨论
// =====================================================================
= 第 5 站 · §5 结果与讨论：从数字到"蛇变水"

== 5.1 瑞士卷：验证"确实在最小化交叉熵"（Table 1）

#orig[
  二维问题里 $p_Y$ 可以用暴力蒙特卡洛评估（对 5 万个无噪瑞士卷样本做核密度估计，带宽 0.2），于是交叉熵 $H[q_theta, p_Y]$ 可以直接算出来当标尺。结果（10 次随机初始化平均）：AffGAN 与 AffDG 的输出轨迹基本贴住后验众数（Figure 1b），交叉熵接近暴力 MAP 解；MSE 与 MAE 显著更差——因为它们本来就不在最小化这个量。带投影的模型"输入 vs 下采样输出"的误差精确为 0，软约束模型训练再久也只能近似满足；且 Aff 系的交叉熵普遍低于对应的 Soft 系。
]

#walk[
  Table 1 数字逐行读（交叉熵越低越好）：MAP 暴力解 3.15 是天花板；*AffDG 3.81、AffGAN 4.10* 贴近天花板；SoftDG 4.19、SoftGAN 4.25 次之——投影稳定优于软约束；*MAE 6.30、MSE 9.10* 远远跑偏。第二列一致性误差：Aff 系精确 0.0，Soft 系在 $10^(-2)$ 到 $10^(-1)$ 量级。两列合起来支撑两个结论：*GAN/去噪引导确实在最小化目标交叉熵；硬投影全面优于软约束*。#sl[24]
]

#pit[
  有趣的细节：二维玩具里 AffDG（3.81）比 AffGAN（4.10）还好——去噪引导在低维时非常能打；它输给 GAN 是*高维*的事（§5.3），原因见附录 E。别把"AffGAN 最佳"说成放之四海皆准。
]

== 5.2 仿射投影不损害性能（Figure 2）

#orig[
  加投影等于限制函数类，需要验证不掉点。做法：在 CelebA 上用 MSE 目标训练带/不带投影的同架构 CNN。发现：带投影的网络随机初始化时起步损失就更低（低频分量天生对齐目标），训练普遍更快，最终 MSE/SSIM 往往更好。为定位收益来源又做两个变体：把 $A^+$ 初始化成正确伪逆后设为可训练；以及随机初始化最后的反卷积层再训练。结论：*把 $A^+$ 初始化成正确的 Moore–Penrose 伪逆很关键；初始化正确后，冻结还是继续训练差别不大*。精确投影的网络把"输入 vs 下采样输出"误差压在数值精度附近（Figure 2c），任何其他变体都会破坏一致性。Figure 2d 可视化了 $A$（上）与数值求得的 $A^+$（下）。
]

#walk[
  Figure 2 图例的二元组 $(dot, dot)$ 要会读：第一位 F/T = 投影层固定（Fixed）还是可训练（Trainable）；第二位 T/R = 初始化用训练好的伪逆（Trained）还是随机（Random）。四条曲线里 (F,T) 与 (T,T) 几乎重合且最好，(T,R) 与无投影最差——正是"初始化关键、冻不冻随意"的图形证据。#sl[25]
]

#idea[
  这一节回答的是审稿人必问的"你的约束会不会伤性能"：不仅不伤，还因为"低频不用学了"而训得更快。附赠一个观察：$A^+$ 核长着负瓣（类似锐化核），直观解释了为什么它不是 $A$ 的转置。
]

== 5.3 草地纹理：AffGAN 胜出，两个"正统"方法出局（Figure 3）

#orig[
  随机纹理用 MSE 损失出名地难。4× 草地实验中：未训练的投影模型（Aff#sub[init]，即纯 $A^+ x$ 基线）已给出低频正确的输出；AffGAN 的结果明显最锐利，且作者认为在给定 LR 输入下是可信的——重建不是逐像素还原，但*统计性质正确*，人眼一看就认得是草。AffDG 与 AffLL 的结果都很模糊，换了多种优化手段也救不回来；据此作者停止这两个模型的后续实验，集中火力做 AffGAN（讨论移入附录 E）。
]

#idea[
  "统计性质正确、逐像素不同"是 GAN 系超分的精髓：纹理类内容本来就没有"唯一正确答案"，评价标准应该是"分布对不对"而不是"每个像素对不对"。这句话也是理解 §5.4 感知–失真权衡的钥匙。#sl[26]
]

== 5.4 CelebA 人脸：感知–失真权衡（Figure 4 + Table 2）

#orig[
  不同损失训练的同架构模型对比：MSE 系输出平滑发糊但"安全"；AffGAN 与 SoftGAN 都明显更锐利，其中 AffGAN 略锐于 SoftGAN、但高频噪点也略多；软约束模型还出现颜色漂移。定量上（Table 2）：按 PSNR / SSIM，MSE 系最高——AffMSE 达 SSIM 0.91 / PSNR 26.53，为全表之冠；AffGAN 为 0.81 / 23.02，高于 SoftGAN 的 0.76 / 21.11。一致性一列：AffMSE $1.6 times 10^(-10)$、AffGAN $9.1 times 10^(-10)$，比 Soft 系（$10^(-3)$ 到 $10^(-5)$ 量级）好出多个数量级。
]

#walk[
  读这张表的正确姿势：*两套指标在打架*——失真指标（PSNR/SSIM，衡量逐像素/结构接近度）说 MSE 系赢，视觉锐利度说 GAN 系赢。这不是矛盾，而是后来被 Blau & Michaeli（2018）形式化的"感知–失真权衡"的一个早期实例：把多解平均能刷高 PSNR，却必然牺牲锐度。#sl[27]
]

#pit[
  措辞红线：本文*没有做人类偏好评测*。所以"AffGAN 更好看"的严谨说法是"视觉上更锐利、纹理更丰富，但 PSNR/SSIM 更低；两类指标衡量的东西不同"。把 PSNR 低说成"输了"或把锐利说成"人类验证更好"都是过度解读。
]

== 5.5 ImageNet：整体可信，但会"梦见"不存在的东西（Figure 5）

#orig[
  用 AffGAN 在 ImageNet 上做 32×32 → 128×128 的 4× 超分：多数结果锐利、与 LR 输入对应良好，但仍带有 GAN 常见的高频噪声，与真图可以区分。最有趣的失败案例：第三列的蛇被超分成了水——显然错误，但考虑到 LR 输入，这仍是一张非常可信的图像；而且水纹在图像先验下的密度很可能高于蛇皮，这说明 GAN 模型"梦出"的是合理的数据。
]

#idea[
  "蛇变水"是全文最好的一页 PPT 素材：它同时展示了方法的能力（生成先验下高概率的内容）与风险（高概率 ≠ 真实发生）。MAP 类方法追求"可信"，不承诺"还原"——医疗、取证等场景要格外小心这一点。#sl[28]
]

== 5.6 自我批评：MAP 的两个原则性问题

#orig[
  *问题一：众数依赖表示。*对随机变量做可逆变换后再做 MAP，答案可能随变换而变。极端例子：把连续标量 $Y$ 用它自己的累积分布函数 $F$ 变换，得到的 $F(Y)$ 服从均匀分布——均匀分布里*每个点都是众数*。因此"MAP 估计"在允许换表示时不唯一，本文在 24 位 RGB 像素表示下求的众数并无特殊地位；换成 CNN 特征空间、甚至只是换个色彩空间，都可能得到不同的解。有趣的是 AffGAN 对坐标变换更有韧性：其目标里多出的 $H[q_theta]$ 项与 $H[q_theta, p_Y]$ 受变换的影响方式相同。

  *问题二：众数未必典型。*众数按定义在高概率区，但不保证长得像随机样本。例：$d$ 维标准高斯，由测度集中，典型样本的范数以极高概率约为 $sqrt(d)$，而众数（原点）范数为 0——在这个意义上众数高度*非典型*。人类观察者能轻易分辨"众数"与"典型样本"，却难以分辨两个随机样本。这提示：从后验 $p_(Y|X)$ *采样*可能是获得可信重建的更好方式。给生成器额外喂噪声即可把 AffGAN 扩展成近似贝叶斯推断（类似 Denton 等 2015），附录 F 证明这个随机版可视作摊销变分推断（如 VAE，Kingma & Welling 2014）。
]

#walk[
  两个论证各补一步直觉：

  *CDF 例子*——概率密度在可逆变换下要乘雅可比 $|F'|^(-1)$，密度的"峰"会被拉平或搬家，所以"密度最高点"不是变换不变量。这不是技术瑕疵，是"用密度峰值定义最优解"这一思路的固有属性。

  *肥皂泡*——高维球壳的体积随半径暴涨（$prop r^(d-1)$），密度虽在原点最高，但"密度 × 体积"的质量几乎全部集中在半径 $approx sqrt(d)$ 的薄壳上。类比：氢原子电子密度在核处最大，但你几乎不可能在核上找到电子。这个比喻出自作者 Huszár 的博客（"高维高斯是肥皂泡"）。#sl[30]
]

#pit[
  这节是作者主动写的"反方论证"，讲的时候千万别跳过——它不但诚实，还为附录 F 与整个"从后验采样"的后续研究方向（也回连到 PRSR 这类自回归采样方法）做了铺垫。跳过它，结尾的变分推断就成了无源之水。
]

// =====================================================================
// §6 结论
// =====================================================================
= 第 6 站 · §6 结论：一段话与三个定位

#orig[
  本文为 SR 的近似 MAP 推断开发了方法：先以架构限制把网络输出投影到有效解的仿射子空间，再给出 GAN、去噪、密度模型三种摊销 MAP 推断方案。高维实验表明 GAN 版本（AffGAN）视觉效果最佳。本工作接续了 GAN 系算法在图像 SR 上的成功实践（Ledig 等 2016），并补充了"为什么该这么做"的理论动机。未来工作聚焦 AffGAN 的随机扩展——可视作摊销变分推断。
]

#idea[
  给论文历史定位的三句话（答辩/讨论可用）：① 它是"GAN 做超分"从工程直觉走向概率解释的关键一步；② 它的实例噪声与"支撑不重叠"诊断成为后续 GAN 稳定性研究（WGAN 一脉）的常引先声；③ 它的去噪引导思想与后来的 score-based / 扩散模型同源。#sl[33]
]

// =====================================================================
// 附录精读
// =====================================================================
= 第 7 站 · 附录精读：证明与细节的白话版

#flow[
  六个附录做四件事：A 补 GAN=KL 的证明；B 补伪逆怎么算；C 展开实例噪声的病理分析；D/E 补实验细节与两个落败方法的尸检；F 给出通往变分推断的扩展。按重要性排序：A ≈ F > C > E > B > D。
]

== 附录 A：为什么"修正 GAN"= 最小化反向 KL（式 13–20）

#orig[
  *第一步：最优判别器长什么样。*固定生成器 $G$，判别器最大化 $bb(E)_(y tilde.op p_Y) log D(y) + bb(E)_(y tilde.op q_G) log(1 - D(y))$。逐点看这是 $a log t + b log(1 - t)$ 型函数（$a = p_Y (y)$，$b = q_G (y)$），最大值在 $t = a\/(a + b)$，故（假设两类先验概率相等）：
]
#peq[16][$ D^* (y) = frac(p_Y (y), p_Y (y) + q_G (y)) $]
#orig[
  *第二步：代入生成器目标。*从 KL 的定义出发：
]
#peq["18–20"][$ "KL"[q_G bar.v.double p_Y] = bb(E)_(y tilde.op q_G) log frac(q_G (y), p_Y (y)) = bb(E)_(y tilde.op q_G) log frac(1 - D^*(y), D^*(y)) approx -bb(E)_(y tilde.op q_G) log frac(D_psi (y), 1 - D_psi (y)) $]
#orig[
  中间等号把 $D^*$ 的表达式反解 $q_G\/p_Y = (1 - D^*)\/D^*$ 后代入；最后一步用训练出的判别器 $D_psi$ 近似 $D^*$。末端表达式恰好就是式 (10) 中生成器最小化的目标——所以在"判别器每步训到最优且唯一"的假设下，生成器的更新就是在对 $"KL"[q_G bar.v.double p_Y]$ 做梯度下降。该假设的现实性正是附录 C 的主题。
]

#walk[
  $a log t + b log(1-t)$ 的极值一行验证：求导 $a\/t - b\/(1-t) = 0 arrow.r.double t = a\/(a+b)$。整个证明只有两个机关：①最优判别器是"真假密度的比例"；②对数几率 $log D\/(1-D)$ 在最优点恰好等于 $log p_Y\/q_G$——*判别器的 logit 就是对数似然比的估计器*。这句话是理解一切 "GAN 当密度比估计器用" 文献的万能钥匙。
]

== 附录 B：伪逆的数值求解与梯度（式 21–25）

#orig[
  $A$ 实现为固定高斯核的步长卷积；$A^+$ 实现为转置卷积，参数 $B$ 用 SGD 最小化 $ell_1 (B) = bb(E)_y norm(A y - A B A y)_2^2$ 与 $ell_2 (B) = bb(E)_x norm(B x - B A B x)_2^2$ 之和（$y, x$ 取标准高斯随机向量）——这是对"$A - A B A$ 与 $B - B A B$ 的谱范数"的蒙特卡洛估计，可当作一个三层全线性卷积网络（$A$ 固定、$B$ 可训）来训练。收敛时 $ell_1 + ell_2$ 在 $10^(-12)$ 到 $10^(-8)$ 之间。注意某些下采样核的精确 $A^+$ 具有无限大的核，只能用局部核近似。梯度方面（式 24–25）：投影后模型对 $theta$ 的梯度等于 $(I - A^+ A) frac(partial g_theta, partial theta)$——即*普通梯度的高通滤波版本*。
]

#idea[
  两条 Moore–Penrose 性质 $A B A = A$、$B A B = B$ 被直接翻译成了两个可微损失——"定义即目标函数"，简洁漂亮。梯度=高通滤波也呼应了直觉：低频由 $A^+ x$ 包办，网络的学习信号天生只含高频。
]

== 附录 C：实例噪声的病理学（含 Figure 6）

#orig[
  考察一个理想化 GAN 算法：每轮①把判别器 $D$ 训到收敛；②从 $D$ 提取对数似然比估计 $s(y) = log q_theta (y) \/ p_Y (y)$；③对 $bb(E)_(y tilde.op q_theta) s(y)$ 做一步梯度更新。若两分布在低维空间中良态，这就是对 KL 的梯度下降，应当收敛。它在实践中失败依赖三个假设的破产：(1) 对数似然比有限；(2) JS 散度是 $theta$ 的良态函数；(3) 逻辑回归的贝叶斯最优解唯一。当 $q_theta$ 与 $p_Y$ 集中且支撑不重叠时——图像建模中几乎必然如此——(1) KL 无穷；(2) JS 饱和、局部常数；(3) 存在一大类损失接近最优的判别器，每个给生成器的梯度都不同，训练随初始化漂移。常见缓解手段是"削弱判别器"：少训几步、或加标签噪声/单边标签平滑。但 Figure 6b 显示标签平滑只是抬高了所有分类器的损失，*近优判别器集合依然又大又杂*——若贝叶斯最优分类器本就不唯一，标签平滑无济于事。实例噪声（Figure 6c）改为给样本加噪：两分布的支撑同时变宽、开始重叠，最优判别器变得唯一、判别器因训练分布更宽而更难过拟合、对数似然比变得良态、加噪后的 JS 不再是 $theta$ 的常数。若 $p_sigma$ 为高斯，则 $d_sigma$ 是 Bregman 散度，且为 0 当且仅当两分布相等。附带的工程红利：不必再担心"把 D 训过头"，每轮可以放心多训判别器。噪声调度是关键超参：本文用加性高斯噪声、按"保持最优判别器损失恒定"的启发式退火；重尾或 spike-and-slab 噪声可能更好，但未测试。
]

#pit[
  注意因果顺序：不是"加噪声让 GAN 碰巧变稳"，而是*先诊断（三个假设破产）后开药（逐条修复）*——这正是这节比多数"炼丹技巧"论文更有分量的原因。与扩散模型的区别再强调一次：这里加噪是为了修判别器的病，不构成生成过程。
]

== 附录 D/E：训练细节与两个落败者的"尸检"（Figure 7）

#orig[
  *D 补充的关键细节*：软约束模型的额外损失是 $ell_"MAE" = 1/N sum_i norm(x_i - A hat(y)_i)$；DAE 预训练最小化 $norm(y - f_"DAE"^sigma (y + epsilon))^2$（式 27），期间退火 $sigma$ 并连续保存参数；训练生成器时按式 (28–30) 用 DAE 梯度做上升，且不断加载更低噪声水平的 DAE 参数——早期用大噪声版本（方向粗、覆盖广），后期用小噪声版本（贴近流形、方向准）。密度模型按链式分解最大化似然（式 31），生成器则最小化生成样本在其下的负对数似然（式 32）。

  *E 的尸检结论*：Figure 7 显示 AffDG / AffLL 的 PSNR、SSIM 都在收敛——模型没有崩，只是收敛到糊的解；AffDG 曲线的台阶状正是"换用更低噪声 DAE"留下的痕迹。两者难点同源：AffDG 的梯度在大小噪声之间两难（准 vs 广），退火不慎就发散；AffLL 则因为准确的密度模型在数据流形附近极度尖锐，训练初期几乎拿不到有用梯度。密度模型本身达到 $-4.10$ bits/dim（与 Theis & Bethge 2015 在纹理数据上的结果相当，是合格的密度模型），但 AffLL 的模糊输出在它之下拿到了 $> -3.5$ 的高似然——*连糊图都给高分*，说明它对 $p_Y$ 的刻画不够精确，当不了可靠的打分器。
]

#idea[
  bits/dim 小知识：密度模型常用"平均每维多少比特"报告负对数似然（越接近 0 越好，此处为负值记法则越大越好）。"糊图也拿高分"这个现象值得记住——*似然高不等于感知质量好*，这也是后来大家意识到"似然与样本质量脱钩"的早期证据之一。
]

== 附录 F：随机版 AffGAN = 摊销变分推断（式 36–47）

#orig[
  给生成器在 LR 输入之外再喂独立噪声 $z tilde.op p_Z$，输出 $hat(y) = Pi_x^A f_theta (x, z)$。定义三个分布：边缘 $q_(Y;theta)$（对 $x, z$ 都平均）、条件 $q_(Y|X;theta)$（固定 $x$、只对 $z$ 平均）、联合 $q_(X,Y;theta) = p_X dot q_(Y|X;theta)$。关键一步：*因为仿射投影保证 $x$ 与 $hat(y)$ 恒一致，联合分布中"给定 $y$ 时 $x$ 的条件"与真实似然 $p_(X|Y) = delta(x - A y)$ 完全相同*，于是 $q_(X,Y;theta) = q_(Y;theta) dot p_(X|Y)$ 与 $= p_X dot q_(Y|X;theta)$ 两种分解同时成立（式 41）。对其应用贝叶斯规则并整理（式 42–46），得到：
]
#peq[47][$ argmin_theta "KL"[q_(Y;theta) bar.v.double p_Y] = argmin_theta bb(E)_(x tilde.op p_X) "KL"[q_(Y|X;theta) bar.v.double p_(Y|X)] $]
#orig[
  左边正是 AffGAN 算法最小化的目标（把边缘输出分布推向真实图像分布），右边是*摊销变分推断*的标准目标：让每个 $x$ 的"近似后验" $q_(Y|X;theta)$ 逼近真实后验 $p_(Y|X)$——而这一切只需要来自 $p_Y$ 与 $p_X$ 的样本。
]

#walk[
  式 (47) 的分量在于左右两边"看见的东西"完全不同：左边只涉及*边缘*分布——GAN 判别器只看单张图真不真，从不看配对；右边却是*逐输入的后验匹配*——这是 VAE 式变分推断的核心诉求。等式说明：只要一致性由投影焊死，"训练一个把边缘分布对齐的 GAN"就*自动*在做"把每个条件后验对齐的变分推断"。翻译回应用：同一张低清图配不同的 $z$，就能采出多张互不相同、但都严格满足 $A hat(y) = x$ 的高清解。#sl[32]
]

#idea[
  首尾呼应：§5.6 的批评（众数非典型 → 应该采样）在这里得到解法雏形；而"从条件分布采样"正是 PRSR 这类自回归方法的思路——两条技术路线在终点汇合。与 VAE 的诚实对比：这里得到的是*近似*后验的样本，不保证真实后验的精确样本。
]

#pit[
  推导成立的隐含前提仍是那件事：*仿射投影保证一致性*。没有投影（Soft 系），式 (41) 的两种分解不再同时成立，整个"边缘 KL = 平均条件 KL"的等式就断了。可以说投影层在本文里"一鱼三吃"：满足似然约束（§3.1）、化归交叉熵（式 9）、支撑变分解释（附录 F）。
]

// =====================================================================
// 收尾
// =====================================================================
= 终点站 · 自测清单与配套材料索引

== 读完自测（答不上就回到对应站）

#block(
  fill: rgb("#f5f8fc"), stroke: 0.6pt + rgb("#c9d4e0"), radius: 4pt, inset: 11pt, width: 100%,
)[
  #set text(size: 9.8pt)
  #set par(first-line-indent: 0em, leading: 0.75em)
  ① 为什么 MSE 训练的超分"注定"发糊？（§1，贝叶斯最优 = 后验均值）\
  ② 式 (7) 的两项各是什么？为何 $A g_theta (x) = x$ 对任意 $theta$ 成立？（§3.1）\
  ③ $A^+$ 是怎么得到的？它是 $A^tack.b$ 吗？$A$ 本身被训练吗？（§3.1、附录 B、§5.2）\
  ④ 式 (9) 为什么说明"不再需要成对数据"？$q_theta$ 是什么分布？（§3.1 续）\
  ⑤ GAN 实际最小化的散度与交叉熵差在哪一项？这一项带来什么行为？（§3.2）\
  ⑥ 实例噪声治的是什么病？为什么单边标签平滑治不了？（§3.2.1、附录 C）\
  ⑦ "去噪残差 ÷ $sigma^2$ ≈ score"的适用条件是什么？训练时怎么处理？（§3.3）\
  ⑧ Table 1 里哪两个数字证明"硬投影优于软约束"？（§5.1）\
  ⑨ Table 2 里 PSNR 最高的是谁？这与"AffGAN 视觉最好"矛盾吗？（§5.4）\
  ⑩ "蛇变水"说明方法的什么性质？什么场景要警惕？（§5.5）\
  ⑪ MAP 的两个原则性缺陷分别是什么？各用什么例子论证？（§5.6）\
  ⑫ 式 (47) 左右两边各是什么目标？等式成立靠什么前提？（附录 F）
]

== 论文章节 ↔ 配套材料对照

#table(
  columns: (auto, auto, 1fr),
  inset: 6pt, stroke: 0.5pt + rgb("#c9d4e0"), align: left,
  table.header([*论文位置*], [*幻灯片*], [*本稿站点*]),
  [标题/摘要], [第 1–2 页], [第 0 站],
  [§1 引言 + Figure 1], [第 4–8 页], [第 1 站],
  [§2 相关工作], [第 16 页（对比 SRGAN）], [第 2 站],
  [§3 理论（式 1–9）], [第 10–14 页], [第 3 站 3.0–3.1],
  [§3.2 AffGAN + 实例噪声], [第 15–19 页], [第 3 站 3.2–3.2.1],
  [§3.3–3.4 AffDG / AffLL], [第 20–22 页], [第 3 站 3.3–3.4],
  [§4–§5.5 实验], [第 23–28 页], [第 4–5 站],
  [§5.6 批评 + §6 结论], [第 30、33 页], [第 5 站 5.6、第 6 站],
  [附录 A–F], [第 18、19、31、32 页], [第 7 站],
)

#v(0.5em)
#align(center)[
  #text(size: 9.5pt, fill: rgb("#8593a3"))[
    —— 全稿完 ·  配套：AffGAN_zh.pdf（逐句译文）· index.html（34 页幻灯片）· 演讲稿\_朗读版.md · 快速入门.html ——
  ]
]
